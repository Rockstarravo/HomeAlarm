import 'package:flutter/material.dart';

/// The one color decision this app makes: everything else (surfaces, text,
/// error/success tones) is derived from this seed by Material 3's dynamic
/// color scheme (see [AppTheme]). Change this one line to re-theme the
/// whole app.
class AppColors {
  const AppColors._();

  static const seed = Color(0xFF3F51B5);
}
